function isHist=isHistogram(X)

isHist=~any(isnan(X(:))||)&&allPositive(X)&&sumsToOne(X);

end

function allPos=allPositive(X)
allPos=all(X>=0);
end

function sums=sumsToOne(X)
n=size(X,1);

sums=all(abs(1-sum(X,1))<eps(1.0)*n*2);
end